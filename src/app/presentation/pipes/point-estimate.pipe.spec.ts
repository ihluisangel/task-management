import { PointEstimatePipe } from './point-estimate.pipe';

describe('PointEstimatePipe', () => {
  it('create an instance', () => {
    const pipe = new PointEstimatePipe();
    expect(pipe).toBeTruthy();
  });
});
